# Where the frontend should send /token, /ask, etc.
# In local dev, leave this unset — Vite proxies /api → http://localhost:3001
# In production (Vercel), set this to your Railway backend URL, e.g.:
#   VITE_BACKEND_URL=https://meet-app-production.up.railway.app
VITE_BACKEND_URL=
